---
---

<section class="section">
  <div class="container vp-grid">
    <div
      class="card"
      style="padding:0; overflow:hidden; aspect-ratio:16/9; border:1px solid var(--border); background:var(--surface);"
    >
      <iframe
        title="HODL Alpha overview"
        width="100%"
        height="100%"
        src="https://www.youtube.com/embed/JmEoVDts7oY?start=378&rel=0&modestbranding=1"
        allow="autoplay; encrypted-media; picture-in-picture"
        allowfullscreen
        style="border:0; display:block;"
      ></iframe>
    </div>

    <div class="card card-accent">
      <div class="badge">Result-driven approach</div>
      <p class="muted" style="margin-top:12px;">
        We help Bitcoin HODLers maximize their long-term returns by optimizing every buy entry.
        Instead of changing your HODL strategy, we enhance it by showing you how to time your buys
        smarter. This allows your portfolio to compound more efficiently. Simply put, if you’re
        committed to HODLing, and looking to get the most out of your strategy — we are the edge
        you’ve been missing.
      </p>
    </div>
  </div>
</section>

<style>
  .vp-grid {
    display: grid;
    gap: clamp(16px, 2.5vw, 24px);
    grid-template-columns: 1.2fr 1fr;
  }
  @media (max-width: 1024px) {
    .vp-grid {
      grid-template-columns: 1fr;
    }
  }
  .card-accent {
    border: 1px solid var(--border);
    background:
      linear-gradient(0deg, rgba(255, 255, 255, 0.02), rgba(255, 255, 255, 0.02)),
      var(--surface);
    padding: 24px;
    border-radius: var(--radius);
    min-height: 100%;
  }
</style>
